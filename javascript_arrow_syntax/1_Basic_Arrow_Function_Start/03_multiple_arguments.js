function multiply(x, y) {
    return x * y;
}

const multiplyAr = (x,y) => {
    return x * y;
}

const multiplyArConcise = (x,y) => x * y;

function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}